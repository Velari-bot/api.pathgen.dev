name = "pathgen-gateway"
main = "worker/index.mjs"
compatibility_date = "2024-03-29"

[vars]
# Origin needs to be the real backend server URL
ORIGIN_URL = "https://api.pathgen.dev" 

# Secrets should be moved to Cloudflare Secrets (wrangler secret put)
# JWT_SECRET = "secret"
